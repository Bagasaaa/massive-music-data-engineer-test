# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Secrets — API keys must never be committed
.env
*.key
config.local.py

# Build artifacts that are regenerated by `python -m src.pipeline`.
# Kept OUT of git so the repo stays small; reviewers regenerate them in one command.
data/raw/*.json
data/warehouse.db
